const ASSIGN_DOCTOR = "ASSIGN_DOCTOR";

const GET_ASSIGNED_PATIENTS = "GET_ASSIGNED_PATIENTS";

const assignState = {
  loading: null,
  assigned: null,
  patients: {},
};

export { ASSIGN_DOCTOR, GET_ASSIGNED_PATIENTS, assignState };
